# NVIDIA AI API Key
nvidia.api.key=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

spring.application.name=devops
server.port=9090

# Database Configuration
spring.datasource.url=jdbc:postgresql://mydb.czq6yecyq39m.eu-north-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=mani
spring.datasource.password=9063589233
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.default_schema=aws
spring.jpa.show-sql=true
spring.jpa.properties.jakarta.persistence.schema-generation.create-database-schemas=true
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# AWS EKS Configuration (optional — auto-detected from your AWS CLI session)
# If blank, region comes from: aws configure get region
# If blank, cluster comes from: kubectl config current-context
aws.region=
aws.eks.cluster-name=

# Kubectl Command Templates
kubectl.commands.get-pods=kubectl get pods -n {namespace}
kubectl.commands.get-services=kubectl get services -n {namespace}
kubectl.commands.get-deployments=kubectl get deployments -n {namespace}
kubectl.commands.get-nodes=kubectl get nodes
kubectl.commands.get-namespaces=kubectl get namespaces
kubectl.commands.get-logs=kubectl logs {podName} -n {namespace}
kubectl.commands.describe-pod=kubectl describe pod {podName} -n {namespace}
kubectl.commands.get-events=kubectl get events -n {namespace}
kubectl.commands.get-configmaps=kubectl get configmaps -n {namespace}
kubectl.commands.describe-configmap=kubectl describe configmap {configmapName} -n {namespace}
kubectl.commands.get-secrets=kubectl get secrets -n {namespace}
kubectl.commands.describe-secret=kubectl describe secret {secretName} -n {namespace}
kubectl.commands.restart-deployment=kubectl rollout restart deployment {deploymentName} -n {namespace}
kubectl.commands.scale-deployment=kubectl scale deployment {deploymentName} --replicas={replicas} -n {namespace}
kubectl.commands.rollout-status=kubectl rollout status deployment {deploymentName} -n {namespace}
kubectl.commands.delete-pod=kubectl delete pod {podName} -n {namespace}
kubectl.commands.top-pods=kubectl top pods -n {namespace}
kubectl.commands.top-nodes=kubectl top nodes
kubectl.commands.get-ingress=kubectl get ingress -n {namespace}
kubectl.commands.get-hpa=kubectl get hpa -n {namespace}

# Hazelcast Caching enabled via HazelcastConfig.java
